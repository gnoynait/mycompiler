#include<global.h>
#incldde"log.h"
FILE * logfile;

void log_init(){
  logfile = fopen("log.txt","w");
  if(logfile == NULL){
    printf("\ncan not open the log file.\n");
    printf("program will exit.");
    getchar();
  }
}

void info(char *str){
  fprintf(logfile, str);
  printf(str);
}
